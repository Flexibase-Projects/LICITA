// HeatmapLayer foi substituído pelo mapa coroplético integrado ao StateLayer.
// As cores dos estados agora são pintadas diretamente no shape do GeoJSON (choropleth).
// Este arquivo não é mais utilizado — mantido apenas para evitar erros de import em builds antigos.
export {}
